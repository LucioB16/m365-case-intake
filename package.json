{
  "name": "m365-case-intake",
  "version": "1.0.0",
  "description": "M365 Copilot automated case intake skill",
  "main": "scripts/run_batch.js",
  "scripts": {
    "setup": "node scripts/setup.js",
    "batch": "node scripts/run_batch.js",
    "fetch": "node scripts/run_fetch.js",
    "prompt": "node scripts/build_prompt.js",
    "environments": "node scripts/init_environments.js",
    "normalize": "node scripts/normalize_agents.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "parse": "node -e \"const { parsePayload } = require('./scripts/lib/parser.js'); const config = require('./config.json'); const r = parsePayload('./logs/latest_response.md', config.ONEDRIVE_FOLDER); process.exit(r.status === 'INVALID' ? 1 : r.status === 'PARTIAL' ? 2 : 0);\""
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "inquirer": "^8.2.6"
  }
}
